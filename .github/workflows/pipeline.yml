name: Controlled Training Pipeline (Train Fails)

on:
  push:
    branches: [ main ]

jobs:
  # 1. LINTER (SUCCESS)
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install linter
        run: pip install flake8

      - name: Run linter
        run: echo "Linter passed successfully"

  # 2. TRAIN (FORCED FAILURE + ARTIFACT)
  train:
    runs-on: ubuntu-latest
    needs: linter
    if: always()

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: echo "Dependencies installed"

      # Create error log 
      - name: Train model
        run: |
          echo "Simulating training failure..." | tee error_logs.txt
          exit 1

      # Upload artifact ONLY if failure happens
      - name: Upload error logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: error-logs
          path: error_logs.txt

      # Always cleanup
      - name: Cleanup
        if: always()
        run: echo "Cleaning up temporary cloud volumes..."
